# Ink_Canvas_Better.Logging

This library provides logging capabilities for applications.

The `FileLogger` class allows logging messages to a specified file.
The `ConsoleLogger` class allows logging messages to the console output.
The `CompositeLogger` class allows combining multiple logger instances to log messages to multiple destinations simultaneously.(async)
